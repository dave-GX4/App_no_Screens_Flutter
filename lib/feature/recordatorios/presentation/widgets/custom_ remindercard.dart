import 'package:flutter/material.dart';

class CustomReminderCard extends StatelessWidget{
  const CustomReminderCard({super.key});

  @override
  Widget build(BuildContext context){
    return Card(
      child: Text('Custom Reminder Card'),
    );
  }
}