name: recordatorios_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # States
  provider: ^6.0.0

  # Routing
  go_router: ^13.0.0

  #Persistencia de Datos
  shared_preferences: ^2.5.3

  # Proteccion de captura
  no_screenshot: ^0.3.1

  # Generate ID
  uuid: ^4.5.1

  : ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/image/